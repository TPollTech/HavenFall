'use strict';

const researchDefs = { ...baseResearchDefs };
const researchOrder = [...baseResearchOrder];

const priorityDefs = { ...basePriorityDefs };
const priorityOrder = [...basePriorityOrder];

const objectDefs = { ...baseObjectDefs };
const buildDefs = { ...baseBuildDefs };
const itemDefs = { ...baseItemDefs };
const recipeDefs = { ...baseRecipeDefs };
const stationLabels = { ...baseStationLabels };
const names = [...baseNames];
